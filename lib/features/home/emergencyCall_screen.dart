import 'package:flutter/cupertino.dart';

class EmergencyCall extends StatelessWidget {
  const EmergencyCall({super.key});

  static const String routeName = "emergencyCallScreen";

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
